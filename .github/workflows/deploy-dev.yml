name: Deploy (dev)

on:
  push:
    branches: ["dev"]
    paths:
      - "functions/**"
      - "deploy.sh"
      - ".env.dev"
      - ".github/workflows/deploy-dev.yml"

jobs:
  plan:
    name: Detect changed functions
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
      deploy_all: ${{ steps.filters.outputs.shared_any_changed }}
    steps:
      - uses: actions/checkout@v4

      # Detect what changed
      - uses: dorny/paths-filter@v3
        id: filters
        with:
          filters: |
            redirector:
              - 'functions/redirector/**'
            upload_processor:
              - 'functions/upload_processor/**'
            delete_campaign:
              - 'functions/delete_campaign/**'
            shared:
              - 'deploy.sh'
              - '.env.dev'
              - 'functions/**/config.dev.sh'

      - id: set-matrix
        run: |
          if [[ "${{ steps.filters.outputs.shared }}" == 'true' ]]; then
            echo "matrix={\"function\":[\"redirector\",\"upload_processor\",\"delete_campaign\"]}" >> $GITHUB_OUTPUT
          else
            list=()
            [[ "${{ steps.filters.outputs.redirector }}" == 'true' ]] && list+=('"redirector"')
            [[ "${{ steps.filters.outputs.upload_processor }}" == 'true' ]] && list+=('"upload_processor"')
            [[ "${{ steps.filters.outputs.delete_campaign }}" == 'true' ]] && list+=('"delete_campaign"')
            if [[ ${#list[@]} -eq 0 ]]; then
              # No relevant changes; keep matrix empty to skip deploy job
              echo 'matrix={"function":[]}' >> $GITHUB_OUTPUT
            else
              echo "matrix={\"function\":[${list[*]}]}" >> $GITHUB_OUTPUT
            fi
          fi

  deploy:
    name: Deploy ${{ matrix.function }} (dev)
    needs: plan
    if: ${{ fromJson(needs.plan.outputs.matrix).function && (join(fromJson(needs.plan.outputs.matrix).function, '') != '') }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.plan.outputs.matrix) }}

    steps:
      - uses: actions/checkout@v4

      # TEMP auth with key (replace with OIDC later)
      - uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.DEV_GCP_SA_KEY }}

      - uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ secrets.DEV_GCP_PROJECT_ID }}

      - name: Deploy via script
        run: ./deploy.sh dev ${{ matrix.function }}
